"""
╔══════════════════════════════════════════════════════════════════╗
║  📖 Хичээл 1.1: Python Орчин Бэлтгэх & "Hello World"          ║
║  Python 102 — JavaScript програмистад зориулсан                 ║
╚══════════════════════════════════════════════════════════════════╝

🎯 Зорилго:
   - Python суулгах, ажиллуулах
   - Virtual environment (venv) ойлгох
   - pip пакеж менежер ашиглах
   - Эхний програмаа бичих

💡 JS програмист та бүхэнд:
   Python дээр {} хаалт байхгүй — ИНДЕНТ (tab/space) ашиглана.
   Мөр төгсгөлд ; (цэг таслал) байхгүй.
   Энэ нь эхэндээ хачин санагдах ч, удалгүй дасна! 😄
"""

# ============================================================
# 📌 ХЭСЭГ 1: Python суулгах
# ============================================================
"""
Python суулгах:
  1. https://python.org/downloads/ хуудаснаас татаж авна
  2. Суулгахдаа "Add Python to PATH" гэсэн checkbox-ийг заавал тэмдэглэ!
  3. Терминалд шалгах:
     python --version    # Python 3.12.x гэж харагдах ёстой
     pip --version       # pip 24.x.x гэж харагдах ёстой

💡 JS-тэй харьцуулалт:
   node --version  →  python --version
   npm --version   →  pip --version
"""


# ============================================================
# 📌 ХЭСЭГ 2: Эхний програм — "Hello World"
# ============================================================

# Python дээр хамгийн энгийн програм:
print("Hello, World! 🌍")

# JS:  console.log("Hello World")
# Py:  print("Hello World")

# Олон зүйл хэвлэх:
print("Сайн байна уу!", "Миний нэр", "Python")
# Гаралт: Сайн байна уу! Миний нэр Python
# print() нь автоматаар хоорондоо зай (space) нэмнэ

# Тусгаарлагч (separator) өөрчлөх:
print("2024", "06", "07", sep="-")
# Гаралт: 2024-06-07

# Мөрийн төгсгөлийг өөрчлөх:
print("Эхний мөр", end=" → ")
print("Хоёр дахь мөр")
# Гаралт: Эхний мөр → Хоёр дахь мөр

# 💡 JS-д console.log() автоматаар шинэ мөрөнд шилжинэ.
#    Python-д ч бас тийм, гэхдээ end="" параметрээр өөрчилж болно.


# ============================================================
# 📌 ХЭСЭГ 3: Коммент (тайлбар)
# ============================================================

# Нэг мөрт тайлбар — # тэмдэгээр
# JS:  // тайлбар
# Py:  # тайлбар

"""
Олон мөрт тайлбар — гурвалсан хашилтаар
JS:  /* олон мөрт тайлбар */
Py:  Дээрх шиг гурвалсан хашилт ашиглана (triple quotes)

Гурвалсан хашилт нь мөн docstring болж ашиглагдана.
Docstring = функц, класс, модулийн тайлбар (JSDoc-тай адилхан).
"""


# ============================================================
# 📌 ХЭСЭГ 4: Virtual Environment (venv)
# ============================================================
"""
Virtual environment = Төслийн тусгаарлагдсан Python орчин

💡 JS-тэй харьцуулалт:
   JS:  node_modules/ хавтас бүх сангуудыг агуулна
   Py:  venv/ хавтас бүх сангуудыг агуулна

Яагаад хэрэгтэй вэ?
   - Төсөл бүр өөр өөр хувилбарын сан (library) ашиглаж болно
   - Системийн Python-ийг "бохирдуулахгүй"
   - Бусад хүнд шилжүүлэхэд амар

Терминалд бичих командууд:

   # 1. venv үүсгэх (ганцхан удаа)
   python -m venv venv

   # 2. venv идэвхжүүлэх
   # Windows:
   venv\\Scripts\\activate
   # Mac/Linux:
   source venv/bin/activate

   # 3. Идэвхтэй эсэхийг шалгах
   # Терминалын эхэнд (venv) гэж харагдана

   # 4. venv-ээс гарах
   deactivate

📝 Анхааруулга:
   venv/ хавтсыг .gitignore-д нэмэхээ мартуузай!
   JS:  .gitignore-д node_modules/ нэмдэг шиг
   Py:  .gitignore-д venv/ нэмнэ
"""


# ============================================================
# 📌 ХЭСЭГ 5: pip — Пакеж менежер
# ============================================================
"""
pip = Python-ийн npm

💡 JS vs Python харьцуулалт:

   npm install express        →  pip install flask
   npm install                →  pip install -r requirements.txt
   npm install --save-dev     →  pip install pytest  (dev dependency тусдаа байхгүй)
   npm list                   →  pip list
   npm uninstall express      →  pip uninstall flask
   npx                        →  python -m (модулийг шууд ажиллуулах)
   package.json               →  requirements.txt эсвэл pyproject.toml
   package-lock.json           →  requirements.txt (хувилбар тогтоосон)

Хэрэгтэй pip командууд:

   pip install requests              # Сан суулгах
   pip install requests==2.31.0      # Тодорхой хувилбар суулгах
   pip install --upgrade requests    # Шинэчлэх
   pip list                          # Суулгасан бүх сангуудыг харах
   pip freeze > requirements.txt     # Одоогийн сангуудыг файлд хадгалах
   pip install -r requirements.txt   # Файлаас суулгах
"""


# ============================================================
# 📌 ХЭСЭГ 6: requirements.txt vs pyproject.toml
# ============================================================
"""
requirements.txt — Хуучин (гэхдээ түгээмэл) арга:
   requests==2.31.0
   flask==3.0.0
   numpy>=1.24.0

pyproject.toml — Орчин үеийн арга (package.json-тай илүү адилхан):
   [project]
   name = "my-project"
   version = "1.0.0"
   dependencies = [
       "requests>=2.31.0",
       "flask>=3.0.0",
   ]

💡 Анхлан суралцагчид requirements.txt ашиглахыг зөвлөе.
   Дараа нь pyproject.toml руу шилжинэ.
"""


# ============================================================
# 📌 ХЭСЭГ 7: Python скрипт ажиллуулах
# ============================================================
"""
Терминалд:
   python 01_hello.py          # Скрипт ажиллуулах
   python -i 01_hello.py       # Ажиллуулаад, интерактив горимд орох (debug-д тустай)
   python                      # Интерактив горим (REPL) — Node REPL-тай адилхан
   python -c "print(1+1)"     # Нэг мөр код ажиллуулах

💡 JS-тэй харьцуулалт:
   node app.js     →  python app.py
   node -e "..."   →  python -c "..."
   node            →  python     (REPL)
"""


# ============================================================
# 📌 ХЭСЭГ 8: Python-ий онцлог шинжүүд (JS-ээс ялгаатай)
# ============================================================

# 1️⃣ Индент (indentation) заавал байх ёстой!
# Python-д {} хаалт байхгүй. Кодын блокийг ИНДЕНТ-ээр тодорхойлно.

if True:
    print("Энэ нь if блокийн дотор")  # 4 зайгаар (space) доторогш
    print("Энэ ч бас дотор")
print("Энэ нь if блокийн гадна")  # Индентгүй = блокоос гарсан

# ⚠️ Буруу индент = SyntaxError! (IndentationError)
# JS-д {} мартсан ч ажилладаг, Python-д индент буруу бол ажиллахгүй.


# 2️⃣ Мөрийн төгсгөлд ; (цэг таслал) байхгүй
x = 10  # Зүгээр л энгийнээр бичнэ. Төгсгөлд ; хэрэггүй.


# 3️⃣ Хувьсагч зарлах түлхүүр үг байхгүй (let, const, var гэх мэт)
name = "Python"       # Шууд л нэр = утга
age = 30              # Өөр нэг хувьсагч
is_awesome = True     # Boolean (том үсгээр True/False)

# 💡 Python-д const (өөрчлөгдөхгүй хувьсагч) гэж байхгүй.
#    Convention: БҮГДИЙГ ТОМ ҮСГЭЭР бичвэл const гэсэн утгатай
MAX_SIZE = 100        # Энэ нь "constant" гэсэн утгатай, гэхдээ өөрчилж болно


# 4️⃣ Динамик хэл (JS шиг)
value = 42            # int (тоо)
value = "hello"       # str (текст) — нэг хувьсагчид өөр төрөл оноож болно
# Python нь JS шиг динамик хэл, гэхдээ Type Hints нэмж болно (TypeScript шиг)


# ============================================================
# 📌 ХЭСЭГ 9: Оролт авах (Input)
# ============================================================

# JS:  readline эсвэл prompt() (browser)
# Py:  input()

# name = input("Таны нэр хэн бэ? ")    # Хэрэглэгчээс оролт авна
# print(f"Сайн байна уу, {name}!")       # f-string ашиглан хэвлэнэ

# ⚠️ input() ҮРГЭЛЖ текст (str) буцаана!
# age = input("Нас: ")        # "25" гэж текст буцаана
# age = int(input("Нас: "))   # int()-ээр тоо руу хөрвүүлнэ


# ============================================================
# 📌 ХЭСЭГ 10: Эхний жинхэнэ програм
# ============================================================

def main():
    """Энгийн мэндчилгээний програм"""
    print("=" * 50)
    print("🐍 Python 102 — Эхний Програм")
    print("=" * 50)

    # Одоогийн огноо, цагийг авах
    from datetime import datetime
    now = datetime.now()
    print(f"📅 Өнөөдөр: {now.strftime('%Y-%m-%d')}")
    print(f"🕐 Цаг: {now.strftime('%H:%M:%S')}")

    # Python-ий хувилбарыг шалгах
    import sys
    print(f"🐍 Python хувилбар: {sys.version}")

    # Платформыг шалгах
    import platform
    print(f"💻 Систем: {platform.system()} {platform.release()}")

    print()
    print("✅ Хичээл 1.1 амжилттай дууслаа!")
    print("👉 Дараагийн хичээл: 02_variables.py")


# ============================================================
# 📌 ХЭСЭГ 11: if __name__ == "__main__" гэж юу вэ?
# ============================================================
"""
💡 JS-тэй харьцуулалт:
   JS-д ийм зүйл байхгүй. Модуль import хийхэд бүх код ажилладаг.
   Python-д зөвхөн шууд ажиллуулахад л main() дуудагдана.

Хэрхэн ажилладаг вэ?
   - python 01_hello.py  → __name__ == "__main__" → main() ажиллана
   - import 01_hello     → __name__ == "01_hello"  → main() ажиллахгүй

Энэ нь яагаад чухал вэ?
   - Модулийг import хийхэд шаардлагагүй код ажиллахаас сэргийлнэ
   - Тест бичихэд хэрэгтэй
   - Python-ий стандарт практик (convention)
"""

if __name__ == "__main__":
    main()


# ============================================================
# 🏋️ ДАСГАЛ АЖИЛ
# ============================================================
"""
✏️ Дасгал 1.1.1:
   Доорх програмыг бичээд ажиллуул:
   - Өөрийн нэрийг хэвлэ
   - Өнөөдрийн огноог хэвлэ
   - "Би Python сурч байна!" гэж хэвлэ

✏️ Дасгал 1.1.2:
   Virtual environment үүсгэж, requests сан суулгаад,
   requirements.txt файл үүсгэ.

   python -m venv venv
   venv\\Scripts\\activate        # Windows
   pip install requests
   pip freeze > requirements.txt

✏️ Дасгал 1.1.3:
   input() ашиглан хэрэглэгчээс нэр асууж, мэндчилгээ хэвлэ.
   Жишээ:
     Таны нэр хэн бэ? Болд
     Сайн байна уу, Болд! Python-д тавтай морил! 🎉
"""
